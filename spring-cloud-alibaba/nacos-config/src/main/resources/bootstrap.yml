spring:
  application:
    name: nacos-config

  cloud:
    nacos:
      config:
        server_addr: http://42.192.154.95:8848
#        prefix: config
        file_extension: yml
        group: DEFAULT_GROUP
        ## 对应命名空间ID
        namespace: